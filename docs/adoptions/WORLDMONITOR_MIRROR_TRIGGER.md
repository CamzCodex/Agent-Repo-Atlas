# World Monitor mirror trigger

Owner-authorised, auditable trigger for importing the current `koala73/worldmonitor:main` source tree into `vendor/worldmonitor-main`.

The trusted workflow verifies identity and AGPL licensing, records the original upstream commit and tree, relocates upstream GitHub Actions workflows to a disabled preservation path, and writes only the isolated vendor branch.

Sanitised mirror run: 3.
